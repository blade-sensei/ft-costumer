{
  "name": "@blade-sensei/ft-request-url",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dts": "tsc -d --declarationDir dist/lib --emitDeclarationOnly --allowJs --declaration index.js",
    "build:doc": "npm run build:dts && typedoc --options typedoc.js",
    "build:doc:html": "npm run build:dts && typedoc --out docs/html --options typedoc.js --plugin none"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "typescript": "^3.8.2"
  },
  "devDependencies": {
    "jsdoc": "^3.6.3",
    "tsd-jsdoc": "^2.4.0",
    "typedoc": "^0.16.10",
    "typedoc-plugin-markdown": "^2.2.16"
  }
}
